<!-- ----------------------------------------------------------------- -->
<!-- PAGINA CON DATI GIOCATORI PER IL FANTA                            -->
<!--                            11.08.26                               -->
<!-- ----------------------------------------------------------------- -->

<%- include('partials/header.ejs') %>

    <div class="container my-5">
        <div class="row justify-content-center">
            <p>Dati estratti da FantaCalcioPedia: <b>Dati aggiornati al 15/08/2026</b></p>
            <!-- Filtri Client-Side -->
            <div class="row g-4 mb-4">
                <div class="col-md-4">
                    <input type="text" id="searchInput" class="form-control" placeholder="Cerca giocatore...">
                </div>
                <div class="col-md-3">
                    <select id="roleFilter" class="form-select">
                        <option value="">Tutti i Ruoli</option>
                        <% ruoli.forEach(ruolo=> { %>
                            <option value="<%= ruolo %>">
                                <%= ruolo %>
                            </option>
                            <% }) %>
                    </select>
                </div>
                <div class="col-md-3">
                    <select id="teamFilter" class="form-select">
                        <option value="">Tutte le Squadre</option>
                        <% squadre.forEach(squadra=> { %>
                            <option value="<%= squadra %>">
                                <%= squadra %>
                            </option>
                            <% }) %>
                    </select>
                </div>
                <div class="col-md-3">
                    <select id="trendFilter" class="form-select">
                        <option value="">Andamento</option>
                        <% trend.forEach(trendValue=> { %>
                            <option value="<%= trendValue %>">
                                <%= trendValue %>
                            </option>
                            <% }) %>
                    </select>
                </div>
            </div>

            <div class="card standing-card shadow-sm border-0">
                <!-- Tabella Giocatori -->
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle" id="playersTable">
                        <thead class="table-dark">
                            <tr>
                                <th>Ruolo</th>
                                <th>Nome</th>
                                <th>Squadra</th>
                                <th>Punteggio</th>
                                <th>FM 25/26</th>
                                <th>Convenienza</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% giocatori.forEach(g=> { %>
                                <tr data-ruolo="<%= g.Ruolo %>" data-squadra="<%= g.Squadra %>"
                                    data-trend="<%= g.Trend %>" class="clickable-row" onclick="submitForm('<%= g.id %>', '<%= g.Nome %>')">
                                    <td><span class="badge bg-secondary">
                                            <%= g.Ruolo %>
                                        </span></td>
                                    <td class="fw-bold">
                                        <%= g.Nome %>
                                    </td>
                                    <td>
                                        <%= g.Squadra %>
                                    </td>
                                    <td>
                                        <%= g.Punteggio %>
                                    </td>
                                    <td>
                                        <%= g['Fantamedia 2025-2026'] %>
                                    </td>
                                    <td>
                                        <%= Number(g.Convenienza).toFixed(1) %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <form id="hiddenForm" action="/statgiocatore" method="POST" style="display:none;">
        <input type="hidden" name="idPlayer" id="idPlayer">
        <input type="hidden" name="namePlayer" id="namePlayer">
    </form>


    <script>
        const searchInput = document.getElementById('searchInput');
        const roleFilter = document.getElementById('roleFilter');
        const teamFilter = document.getElementById('teamFilter');
        const trendFilter = document.getElementById('trendFilter');
        const rows = document.querySelectorAll('#playersTable tbody tr');

        function filterTable() {
            const search = searchInput.value.toLowerCase();
            const role = roleFilter.value;
            const team = teamFilter.value;
            const trend = trendFilter.value;

            rows.forEach(row => {
                const name = row.children[1].textContent.toLowerCase();
                const rowRole = row.getAttribute('data-ruolo');
                const rowTeam = row.getAttribute('data-squadra');
                const rowTrend = row.getAttribute('data-trend');

                const matchesSearch = name.includes(search);
                const matchesRole = !role || rowRole === role;
                const matchesTeam = !team || rowTeam === team;
                const matchesTrend = !trend || rowTrend === trend;

                row.style.display = (matchesSearch && matchesRole && matchesTeam && matchesTrend) ? '' : 'none';
            });
        }

        searchInput.addEventListener('input', filterTable);
        roleFilter.addEventListener('change', filterTable);
        teamFilter.addEventListener('change', filterTable);
        trendFilter.addEventListener('change', filterTable);


        function submitForm(id, nome,) {
            document.getElementById('idPlayer').value = id;
            document.getElementById('namePlayer').value = nome;
            document.getElementById('hiddenForm').submit();
        }


    </script>
<%- include('partials/footer.ejs') %>